spring:
  application:
    name: eureka-server
  profiles:
    active: @spring.profiles.active@
  cloud:
    config:
      label: develop
      profile: @spring.profiles.active@
      uri: http://localhost:8888/