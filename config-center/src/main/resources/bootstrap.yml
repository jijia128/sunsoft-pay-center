spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jijia128/sunsoft-pay-center/
          searchPaths: respo/*
          label: master
          username:
          password: