spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jijia128/sunsoft-pay-center
          searchPaths: config-file/respo/*,config-file/application/*
          label: develop
          username:
          password: