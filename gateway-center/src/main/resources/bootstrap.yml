spring:
  application:
    name: auth-gateway
  profiles:
    active: @spring.profiles.active@
  cloud:
    config:
      label: develop
      profile: @spring.profiles.active@
      uri: https://localhost:8888/