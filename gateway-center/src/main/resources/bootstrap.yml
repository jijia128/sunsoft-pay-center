spring:
  application:
    name: auth-gateway
  profiles:
    active: @spring.profiles.active@
  cloud:
    config:
      label: develop
      profile: @spring.profiles.active@
      uri: http://localhost:8888/

 